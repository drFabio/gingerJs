var chai=require('chai');
chai.config.includeStack =true;
var expect=chai.expect;
var Ginger=require(__dirname+'/../../../Ginger.js');
var should = chai.should
var ginger;

describe('Component Authentication',function(){
	it('Should not be aple to login with the wrong password',function(){
		var passport=require('passport');
	});
	it('Should be able to login with the right password');
	it('Should show a error if there isn\t an authentication modle');
	it('Should be able to logout');
});